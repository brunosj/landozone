name: Deploy landozone.net

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh/
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.HOST }} >> ~/.ssh/known_hosts

      - name: Execute deployment script
        env:
          HOST: ${{ secrets.HOST }}
          USERNAME: ${{ secrets.USERNAME }}
          BASE_PATH: /home/lando/landozone
          PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
        run: |
          set -e
          echo "🚀 Starting deployment of $PROJECT_NAME..."
          ssh -o StrictHostKeyChecking=no -o LogLevel=ERROR ${{ env.USERNAME }}@${{ env.HOST }} "
            set -e
            
            if [ ! -d '${{ env.BASE_PATH }}/repo' ]; then
              echo '❌ Repo directory not found!'
              exit 1
            fi
            
            cd '${{ env.BASE_PATH }}/repo'
            
            if [ ! -f 'scripts/deploy.sh' ]; then
              echo '❌ Deploy script not found!'
              exit 1
            fi
            
            APP_DIR='${{ env.BASE_PATH }}/repo' APP_PORT=5175 bash scripts/deploy.sh
          "
          EXIT_STATUS=$?
          if [ $EXIT_STATUS -eq 0 ]; then
            echo "✅ Deployment completed successfully"
          else
            echo "❌ Deployment failed with exit status $EXIT_STATUS"
          fi
          exit $EXIT_STATUS

      - name: Verify deployment
        if: success()
        env:
          HOST: ${{ secrets.HOST }}
          USERNAME: ${{ secrets.USERNAME }}
        run: |
          echo "🔍 Verifying deployment..."
          ssh -o StrictHostKeyChecking=no -o LogLevel=ERROR ${{ env.USERNAME }}@${{ env.HOST }} "
            echo '📊 PM2 process check: landozone'
            if ! pm2 describe landozone > /dev/null 2>&1; then
              echo '❌ PM2 process landozone not found'
              exit 1
            fi

            echo '🏥 Health check on :5175'
            curl -sf http://localhost:5175 > /dev/null
            echo '✅ App responding on port 5175'
          "

  notify:
    needs: deploy
    runs-on: ubuntu-latest
    if: failure()

    steps:
      - name: Notify via Resend
        uses: brunosj/actions/resend-notify@v1
        with:
          subject: 'GitHub Actions - Deploy ${{ secrets.PROJECT_NAME }} Failed'
          resend_api_key: ${{ secrets.RESEND_API_KEY }}
